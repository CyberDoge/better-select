import classNames from "classnames";
import React from "react";
import styles from "./ColumnCreator.module.sass";
export const ColumnCreator = ({ className }) => {
  return <div className={classNames(className, styles.container)}>asd</div>;
};
